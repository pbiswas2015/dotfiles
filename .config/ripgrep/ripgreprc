# Settings
--smart-case
# Search hidden files / directories (e.g. dotfiles) by default
--hidden

# Max line length
--max-columns=10000

# Filetypes
#--type-add='src:include:tcl,py'
--type-add
src:*.{tcl,py,pl}

# Using glob patterns to include/exclude files or folders
# Exclude
--glob=!{.git}
--glob=!{.snapshot}
